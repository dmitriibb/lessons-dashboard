# Main Goal
Create a simple backend server on Go

# Details
No need to use specific framework, plain http package is enough, but worth to consider creating a common classes and functions. I ncase if if stick to no-framework concept - we should avoid writing a lot of boilerplate code
Add 1 API GET /hello to return plain string "Hello world"

# Definition of Done
We can run a simple Go backend service

# Restrictions
Don't use framework unless it is really needed.
